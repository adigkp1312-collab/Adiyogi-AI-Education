import type { SupportedLanguage } from "@/types";

export async function textToSpeech(text: string, language: SupportedLanguage): Promise<string> {
    // Placeholder mock for Sarvam AI TTS as the original implementation is missing
    return "data:audio/mp3;base64,...";
}
